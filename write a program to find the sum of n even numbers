#include<stdio.h>
int main()
{
    int num,sum=0;
    printf("Enter the number\n");
    scanf("%d",&num);
    for(int i=1;i<=num;i++)
    {
        if(i%2==0)
        sum+=i;
    }
    printf("sum=%d",sum,num);
    return 0;
}
